name: Tests
on:
  push:
    branches: [ default ]
  pull_request:
    branches: [ default ]

jobs:
  test:
    runs-on: ubuntu-18.04
    steps:
    - uses: actions/checkout@v2
    - name: Run Build
      run: echo Hi
    